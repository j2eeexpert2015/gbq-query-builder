-- All Base Queries 
SELECT DISTINCT 
DC_SEQ 
FROM USAG_INVPLTFRM_SYNC.V_D_DC_RLUP  EXCEPT DISTINCT 
SELECT DC_SEQ 
FROM V_F_DC_PROD_LED;

SELECT DISTINCT 
DC_ABBR_CD, DC_NM 
FROM USAG_LOCATION_SYNC.V_D_DC_RLUP  EXCEPT DISTINCT 
SELECT DC_ABBR_CD, DC_NM 
FROM V_F_DC_PROD_LED;

SELECT DISTINCT 
DC_SEQ, DC_ABBR_CD 
FROM USAG_LOCATION_SYNC.T1  EXCEPT DISTINCT 
SELECT DC_SEQ, DC_ABBR_CD 
FROM V_F_DC_PROD_LED;

SELECT DISTINCT 
DC_NM 
FROM USAG_LOCATION_SYNC.T2  EXCEPT DISTINCT 
SELECT DC_NM 
FROM V_F_DC_PROD_LED;

-- All Self Queries 
SELECT A, B 
CASE WHEN A = B THEN 'False' 
ELSE 'True' 
END 
FROM V_F_DC_PROD_LED;

SELECT C, D 
CASE WHEN C = D THEN 'False' 
ELSE 'True' 
END 
FROM V_F_DC_PROD_LED;

